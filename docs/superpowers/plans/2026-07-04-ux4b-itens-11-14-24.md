# UX 4b/4c — Itens 11, 14 e 24 (título/favicon por mundo, export CSV, empty states)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Três QOL: aba do navegador identifica o mundo (título + favicon monograma na Intranet), botão de export CSV do funil no Kanban, e empty states com próximo passo (coluna vazia + lead sem tese).

**Architecture:** Só frontend. Item 11 = helper `worldChrome.js` (document.title + swap de favicons, no padrão do `faviconHelper.js` existente) chamado por um watcher no `WorldRail.vue` (sempre montado). Item 14 = helper puro `leadsCsv.js` + botão no `KanbanBoard.vue` usando `downloadCsvFile` já existente. Item 24 = hints condicionais no `KanbanColumn.vue` e `LeadFields.vue`.

**Tech Stack:** Vue 3, vitest, i18n `RAMON.*`.

## Global Constraints

- **Tailwind only**; sem bare strings em template (i18n em `pt_BR/ramon.json` E `en/ramon.json`); eventos custom camelCase.
- Rodar vitest da raiz: `npx pnpm@10.2.0 exec vitest run <path>` (se `npx vitest` falhar por cache do npx).
- Commits Conventional Commits, sem referência a Claude. lint-staged roda no pre-commit (eslint --fix em js/vue, prettier em scss).
- Fatos verificados do código (NÃO re-descobrir): lead tem `id, name, contact_phone, lead_stage_id, stage_name, benefit_type_name, lead_priority_name, thesis_id, thesis_name, value, source, sdr_name, closer_name, contact_name, conversation_id, stage_entered_at` (ver `app/models/lead.rb#push_event_data`). `WorldRail.vue` já tem `const isIntranet = computed(() => route.meta?.world === 'intranet')`. Favicons do app: links `<link class="favicon" sizes="...">` com href `/favicon-<sizes>.png` (ver `app/javascript/dashboard/helper/AudioAlerts/faviconHelper.js`). Monograma público: `/brand-assets/ramon-monogram.png`. Helper de download: `dashboard/helper/downloadHelper.js` exporta `downloadCsvFile(fileName, content)`.

## Mapa de arquivos

| Arquivo | Mudança |
|---|---|
| `app/javascript/dashboard/routes/dashboard/ramon/helpers/worldChrome.js` | novo — título+favicon por mundo |
| `app/javascript/dashboard/routes/dashboard/ramon/components/WorldRail.vue` | watcher chamando o helper |
| `app/javascript/dashboard/routes/dashboard/ramon/helpers/leadsCsv.js` | novo — leads → CSV |
| `app/javascript/dashboard/routes/dashboard/ramon/components/kanban/KanbanBoard.vue` | botão Exportar CSV |
| `app/javascript/dashboard/routes/dashboard/ramon/components/kanban/KanbanColumn.vue` | hint de coluna vazia |
| `app/javascript/dashboard/routes/dashboard/ramon/components/lead/LeadFields.vue` | hint de lead sem tese |
| i18n `pt_BR/ramon.json` + `en/ramon.json` | chaves novas |
| specs em `ramon/helpers/specs/` (nova pasta), `kanban/specs/`, `lead/specs/` | testes |

---

### Task 1: Título + favicon por mundo (item 11)

**Files:**
- Create: `app/javascript/dashboard/routes/dashboard/ramon/helpers/worldChrome.js`
- Create: `app/javascript/dashboard/routes/dashboard/ramon/helpers/specs/worldChrome.spec.js`
- Modify: `app/javascript/dashboard/routes/dashboard/ramon/components/WorldRail.vue`

**Interfaces:**
- Produces: `applyWorldChrome(isIntranet: boolean)` — ajusta `document.title` e os hrefs dos `<link class="favicon">`.

**Comportamento:** na Intranet, título = `Intranet · Ramon Antônio` e favicons apontam pro monograma `/brand-assets/ramon-monogram.png`; fora dela, título = `Ramon Antônio` e favicons voltam pro padrão `/favicon-<sizes>.png`. Limitação aceita (documentar em comentário): o badge de notificação do `faviconHelper.js` pode sobrescrever o monograma até a próxima troca de rota — cosmético.

- [ ] **Step 1: Teste que falha**

```js
// app/javascript/dashboard/routes/dashboard/ramon/helpers/specs/worldChrome.spec.js
import { applyWorldChrome } from '../worldChrome';

describe('applyWorldChrome', () => {
  beforeEach(() => {
    document.head.innerHTML =
      '<link class="favicon" sizes="32x32" href="/favicon-32x32.png" />' +
      '<link class="favicon" sizes="96x96" href="/favicon-96x96.png" />';
    document.title = 'qualquer';
  });

  it('na intranet: título próprio e favicon monograma', () => {
    applyWorldChrome(true);
    expect(document.title).toBe('Intranet · Ramon Antônio');
    document.querySelectorAll('.favicon').forEach(l => {
      expect(l.getAttribute('href')).toBe('/brand-assets/ramon-monogram.png');
    });
  });

  it('fora da intranet: título e favicons padrão restaurados', () => {
    applyWorldChrome(true);
    applyWorldChrome(false);
    expect(document.title).toBe('Ramon Antônio');
    expect(
      document.querySelector('.favicon[sizes="32x32"]').getAttribute('href')
    ).toBe('/favicon-32x32.png');
  });
});
```

- [ ] **Step 2: Rodar e ver falhar** — `npx pnpm@10.2.0 exec vitest run app/javascript/dashboard/routes/dashboard/ramon/helpers/specs/worldChrome.spec.js` → FAIL (módulo não existe).

- [ ] **Step 3: Implementar**

```js
// app/javascript/dashboard/routes/dashboard/ramon/helpers/worldChrome.js
// Título e favicon por mundo (item 11 do 4b). O badge de som do
// faviconHelper.js pode sobrescrever o monograma até a próxima troca de
// rota — cosmético, aceito.
const MONOGRAM = '/brand-assets/ramon-monogram.png';

export const applyWorldChrome = isIntranet => {
  document.title = isIntranet ? 'Intranet · Ramon Antônio' : 'Ramon Antônio';
  document.querySelectorAll('.favicon').forEach(link => {
    link.setAttribute(
      'href',
      isIntranet ? MONOGRAM : `/favicon-${link.getAttribute('sizes')}.png`
    );
  });
};
```

(Título é chrome do navegador, não template Vue — string literal é aceitável aqui, como o faviconHelper faz com paths.)

No `WorldRail.vue`, adicionar ao `<script setup>`:

```js
import { watch } from 'vue'; // juntar ao import de 'vue' existente
import { applyWorldChrome } from '../helpers/worldChrome';

watch(isIntranet, applyWorldChrome, { immediate: true });
```

- [ ] **Step 4: Rodar e ver passar** — mesmo comando, PASS (2).

- [ ] **Step 5: Commit** — `git add -A && git commit -m "feat(ramon): titulo e favicon proprios no mundo intranet"`

---

### Task 2: Export CSV do funil (item 14)

**Files:**
- Create: `app/javascript/dashboard/routes/dashboard/ramon/helpers/leadsCsv.js`
- Create: `app/javascript/dashboard/routes/dashboard/ramon/helpers/specs/leadsCsv.spec.js`
- Modify: `app/javascript/dashboard/routes/dashboard/ramon/components/kanban/KanbanBoard.vue`
- Modify: i18n `pt_BR/ramon.json` + `en/ramon.json` (bloco `RAMON.FUNIL`)

**Interfaces:**
- Consumes: `downloadCsvFile(fileName, content)` de `dashboard/helper/downloadHelper`; `orderedStages` e `stageLeads(id)` já existentes no KanbanBoard.
- Produces: `leadsToCsv(leads: Array) => string` (CSV com BOM UTF-8, separador `;` — padrão Excel pt-BR).

- [ ] **Step 1: Teste que falha**

```js
// app/javascript/dashboard/routes/dashboard/ramon/helpers/specs/leadsCsv.spec.js
import { leadsToCsv } from '../leadsCsv';

describe('leadsToCsv', () => {
  it('gera cabeçalho + linhas com ; e BOM', () => {
    const csv = leadsToCsv([
      {
        name: 'João',
        contact_phone: '+554899999',
        stage_name: 'Novo',
        thesis_name: null,
        benefit_type_name: 'Auxílio-acidente',
        lead_priority_name: null,
        value: 9800.5,
        source: 'lp-meta',
        sdr_name: null,
        closer_name: null,
        lost_reason: null,
      },
    ]);
    expect(csv.startsWith('\uFEFF')).toBe(true); // BOM sempre via escape \uFEFF, nunca o caractere literal
    const [header, row] = csv.slice(1).split('\n');
    expect(header).toBe(
      'nome;telefone;etapa;tese;beneficio;prioridade;valor;origem;sdr;closer;motivo_perda'
    );
    expect(row).toBe(
      'João;+554899999;Novo;;Auxílio-acidente;;9800.5;lp-meta;;;'
    );
  });

  it('escapa ; aspas e quebra de linha', () => {
    const csv = leadsToCsv([
      { name: 'A;B', source: 'diz "oi"', stage_name: 'X\nY' },
    ]);
    const row = csv.slice(1).split('\n').slice(1).join('\n');
    expect(row).toContain('"A;B"');
    expect(row).toContain('"diz ""oi"""');
    expect(row).toContain('"X\nY"');
  });
});
```

- [ ] **Step 2: Rodar e ver falhar** — FAIL (módulo não existe).

- [ ] **Step 3: Implementar**

```js
// app/javascript/dashboard/routes/dashboard/ramon/helpers/leadsCsv.js
// Export do funil (item 14 do 4b). Separador ';' + BOM = abre certo no
// Excel pt-BR sem assistente de importação.
const COLUMNS = [
  ['nome', l => l.name],
  ['telefone', l => l.contact_phone],
  ['etapa', l => l.stage_name],
  ['tese', l => l.thesis_name],
  ['beneficio', l => l.benefit_type_name],
  ['prioridade', l => l.lead_priority_name],
  ['valor', l => l.value],
  ['origem', l => l.source],
  ['sdr', l => l.sdr_name],
  ['closer', l => l.closer_name],
  ['motivo_perda', l => l.lost_reason],
];

const cell = value => {
  const s = value == null ? '' : String(value);
  return /[;"\n]/.test(s) ? `"${s.replace(/"/g, '""')}"` : s;
};

export const leadsToCsv = leads => {
  const header = COLUMNS.map(([label]) => label).join(';');
  const rows = leads.map(lead =>
    COLUMNS.map(([, pick]) => cell(pick(lead))).join(';')
  );
  return `\uFEFF${[header, ...rows].join('\n')}`;
};
```

- [ ] **Step 4: Rodar e ver passar** — PASS (2).

- [ ] **Step 5: Botão no KanbanBoard**

No `<script setup>` do `KanbanBoard.vue`:

```js
import { downloadCsvFile } from 'dashboard/helper/downloadHelper';
import { leadsToCsv } from '../../helpers/leadsCsv';

const exportCsv = () => {
  const all = orderedStages.value.flatMap(s => stageLeads(s.id));
  const date = new Date().toISOString().slice(0, 10);
  downloadCsvFile(`funil-${date}.csv`, leadsToCsv(all));
};
```

No template, ao lado do botão "Novo lead" (antes dele), botão secundário:

```html
<button
  data-testid="export-csv"
  class="flex items-center gap-1 px-3 py-1.5 text-sm rounded-lg text-n-slate-11 border border-n-weak hover:text-n-slate-12"
  @click="exportCsv"
>
  <span class="i-lucide-download size-4" />{{ $t('RAMON.FUNIL.EXPORT_CSV') }}
</button>
```

i18n: `pt_BR` → `"EXPORT_CSV": "Exportar CSV"`; `en` → `"EXPORT_CSV": "Export CSV"` (dentro de `RAMON.FUNIL`, irmão de `NEW_LEAD`).

O export respeita os filtros ativos porque `stageLeads` lê o store já filtrado (o `get` refaz a busca com os filtros).

- [ ] **Step 6: Rodar suíte do kanban** — `npx pnpm@10.2.0 exec vitest run app/javascript/dashboard/routes/dashboard/ramon/components/kanban` → PASS (todos).

- [ ] **Step 7: Commit** — `git add -A && git commit -m "feat(kanban): exportar csv do funil filtrado"`

---

### Task 3: Empty states com próximo passo (item 24)

**Files:**
- Modify: `app/javascript/dashboard/routes/dashboard/ramon/components/kanban/KanbanColumn.vue`
- Modify: `app/javascript/dashboard/routes/dashboard/ramon/components/lead/LeadFields.vue`
- Modify: i18n `pt_BR/ramon.json` (+`en`) — `RAMON.KANBAN.COLUMN.EMPTY` e `RAMON.DRAWER.NO_THESIS_HINT`
- Test: specs existentes de `KanbanColumn` e `LeadFields` (adicionar casos)

- [ ] **Step 1: Testes que falham**

`KanbanColumn.spec.js`:

```js
describe('empty state da coluna', () => {
  it('mostra hint quando não há leads', () => {
    const wrapper = mount(KanbanColumn, {
      props: { stage, leads: [] },
      global: { mocks: { $t: k => k } },
    });
    expect(wrapper.find('[data-testid="column-empty"]').exists()).toBe(true);
  });

  it('não mostra hint quando há leads', () => {
    const wrapper = mount(KanbanColumn, {
      props: { stage, leads },
      global: { mocks: { $t: k => k } },
    });
    expect(wrapper.find('[data-testid="column-empty"]').exists()).toBe(false);
  });
});
```

`LeadFields.spec.js` (seguir o padrão de mount do arquivo):

```js
describe('hint de lead sem tese', () => {
  it('mostra o hint quando thesis_id é null', () => {
    // montar com lead sem thesis_id (como os testes existentes montam)
    expect(wrapper.find('[data-testid="no-thesis-hint"]').exists()).toBe(true);
  });
  it('esconde o hint quando o lead tem tese', () => {
    // montar com lead com thesis_id
    expect(wrapper.find('[data-testid="no-thesis-hint"]').exists()).toBe(false);
  });
});
```

- [ ] **Step 2: Rodar e ver falhar.**

- [ ] **Step 3: Implementar**

`KanbanColumn.vue` — logo ANTES do `<Draggable>` (o hint é irmão; o Draggable segue sendo o alvo de drop com seu `min-h-[120px]`):

```html
<p
  v-if="!localLeads.length"
  data-testid="column-empty"
  class="px-3 pt-3 text-xs text-center text-n-slate-9"
>
  {{ $t('RAMON.KANBAN.COLUMN.EMPTY') }}
</p>
```

`LeadFields.vue` — o `DocChecklist` hoje renderiza com `v-if="lead.thesis_id"`. Adicionar, imediatamente após o seletor de tese existente (localizar o select/campo de tese no arquivo), o hint:

```html
<p
  v-if="!lead.thesis_id"
  data-testid="no-thesis-hint"
  class="text-xs text-n-slate-9"
>
  {{ $t('RAMON.DRAWER.NO_THESIS_HINT') }}
</p>
```

i18n `pt_BR`:
- `RAMON.KANBAN.COLUMN.EMPTY`: `"Sem leads aqui — arraste um card ou crie um novo."`
- `RAMON.DRAWER.NO_THESIS_HINT`: `"Escolha a tese para liberar o checklist de documentos e o playbook."`

i18n `en`:
- `EMPTY`: `"No leads here — drag a card or create one."`
- `NO_THESIS_HINT`: `"Pick a thesis to unlock the document checklist and playbook."`

- [ ] **Step 4: Rodar e ver passar** — specs de `KanbanColumn` e `LeadFields` inteiros PASS.

- [ ] **Step 5: Commit** — `git add -A && git commit -m "feat(ramon): empty states com proximo passo no funil e no lead"`

---

### Task 4: Lint + suíte + push

- [ ] **Step 1:** `npx pnpm@10.2.0 exec eslint <arquivos js/vue tocados> --fix` → zero erros (warnings pré-existentes ok).
- [ ] **Step 2:** `npx prettier --write` nos dois `ramon.json`.
- [ ] **Step 3:** `npx pnpm@10.2.0 exec vitest run app/javascript/dashboard/routes/dashboard/ramon` → PASS.
- [ ] **Step 4:** Commit de ajustes se houver (`chore: lint e formatacao`).
